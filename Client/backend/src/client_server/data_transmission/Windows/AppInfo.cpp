#include "../AppInfo.h"

std::optional<std::string> AppInfo::getInstallerVersion() {
  std::string version;

  // Logic will be added after the installer will be done
  // This file is simply required for the basic implementation
  version = "1.0.0";

  return version;
}